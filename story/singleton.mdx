---
name: Singleton Data Provider
---

import { Playground } from 'docz';
import { SingletonDataProvider } from "../src";
import { DataConsumer } from "./mock/consumer";

# Singleton Data Provider

## Loading Node Based

<Playground>
    <SingletonDataProvider
        sources={{
            first: () => new Promise((resolve) => resolve('First Result')),
            second: () => new Promise((resolve) => resolve('Second Result')),
        }}
        loading={<div>Loading</div>}
    >
        <DataConsumer />
    </SingletonDataProvider>
</Playground>
